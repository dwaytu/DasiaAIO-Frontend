# Mobile default points to production backend.
# For local Android testing, override in `.env.mobile.local` (for example `http://10.0.2.2:5000` or your LAN API host).
VITE_API_BASE_URL=https://backend-production-0c47.up.railway.app
